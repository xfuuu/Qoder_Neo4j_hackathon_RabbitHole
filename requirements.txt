mcp>=2.0.0
httpx>=0.28
pydantic>=2.0
neo4j>=5.20
streamlit>=1.38
pyvis>=0.3.2
python-dotenv>=1.0
# Only needed when ANTHROPIC_API_KEY is set; without it the stories go through
# the local Claude Code CLI instead.
anthropic>=0.40
